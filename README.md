# HOW TO RUN
1. Install pipenv  
    ```pip install pipenv```
2. Install dependencies  
    ```pipenv install```
3. Run  
    ```pipenv run python main.py```

# DEPENDENCIES
Look under `[packages]` in `Pipfile`
